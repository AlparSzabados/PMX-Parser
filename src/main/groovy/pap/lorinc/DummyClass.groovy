package pap.lorinc

/*
 * This is the `production` code, i.e. the one that will be shipped to the client.
 * Jump to its `test` class via `Ctrl Shift T` from `Idea`
 */
class DummyClass { // TODO change to your own
    static void main(String... args) {
        // Starting point for application
    }

    static dummyMethod() {
        'Dummy return value'
    }

    static dummyMethod2(int a, int b) {
        a + b
    }
}